📦 qFTD-004 — GATE STATE DESYNC ROOT FIX (ARCHITECTURE VALIDATION)
🧠 OBJECTIVE

Eliminate dual-state inconsistency between:

Orchestrator runtime inputs
Global Gate internal state

And enforce:

✅ Single Source of Truth (SSOT) for readiness

💣 CRITICAL FINDING (CONTEXT FOR DEV)

System currently shows:

Input:
indicator_ok = True
data_fresh = True

Gate evaluates:
_ind_ready = False
_data_fresh = False

👉 This proves:

❗ Gate is NOT using runtime inputs
❗ Gate is using stale/internal state

🔍 SECTION 1 — SOURCE OF TRUTH TRACE (MANDATORY)
Q1.1

Identify ALL places where these are defined/used:

indicator_ready / indicator_ok
data_fresh

👉 Provide:

File + Function + Variable Name
Q1.2

Trace FULL lifecycle:

Where value originates →
Where it is updated →
Where it is consumed →
Where mismatch occurs
Q1.3

Answer clearly:

What is the CURRENT source of truth?

(A) Orchestrator inputs
(B) Global gate internal state
(C) Cached state
(D) Mixed (BUG)
🔍 SECTION 2 — GLOBAL GATE INTERNAL STATE AUDIT
Q2.1

Inside:

core/gating/global_gate_controller.py

👉 List all variables used for readiness:

self._indicator_ready
self._data_fresh
...
Q2.2

For each variable:

Variable	Where Set	When Updated	Source
Q2.3

Check:

Are these values updated every tick?
Or cached/stale?

👉 Answer: YES / NO + proof

🔍 SECTION 3 — PARAMETER FLOW VALIDATION
Q3.1

In:

execution_orchestrator.gate_check()

👉 Confirm:

indicator_ok is passed?
data_fresh is passed?
Q3.2

Trace:

gate_check() → global_gate_controller.evaluate()

👉 Question:

Are these parameters actually USED inside evaluate()?

Q3.3 (CRITICAL)

If NOT used:

👉 show exact line where they are ignored

🔍 SECTION 4 — DATA FRESHNESS PIPELINE
Q4.1

Where is data_fresh computed?

👉 File + function

Q4.2

How is freshness defined?

timestamp delta?
cache TTL?
websocket heartbeat?
Q4.3

Explain why:

input = True
gate = False
Q4.4

Check config:

cache_ttl = 1.0

👉 Is this causing constant stale state?

🔍 SECTION 5 — SAFE MODE DEPENDENCY
Q5.1

Safe mode depends on:

indicator_ready + data_fresh

👉 Confirm?

Q5.2

Does safe mode:

read from gate state?
or independent source?
Q5.3

Is safe mode reinforcing wrong state?

🔍 SECTION 6 — REQUIRED FIX DESIGN (MANDATORY)
Q6.1

Propose FINAL architecture:

Must include:
Single source of truth
No duplicate state
Real-time evaluation
Q6.2

Choose ONE:

OPTION 1 (Preferred)
Gate becomes PURE FUNCTION

evaluate(indicator_ok, data_fresh, ...)

NO internal state

OPTION 2
Central State Manager (SSOT)

All modules read from it
Q6.3

Explain:

Why your design eliminates mismatch permanently

🔍 SECTION 7 — BREAKAGE IMPACT CHECK
Q7.1

Will fix affect:

Safe mode?
Scan controller?
Execution orchestrator?
Q7.2

Any backward compatibility issues?

🔍 SECTION 8 — PROOF TEST (MANDATORY)

After fix, run probe again:

Expected:

gate_open > 0
safe_mode = OFF (when conditions valid)
scan_allowed > 0
execution_called = TRUE
🔥 FINAL INSTRUCTION

❗ DO NOT PATCH
❗ DO NOT FORCE TRUE
❗ FIX ARCHITECTURE

🏁 OUTPUT REQUIRED (aFTD)

Developer MUST return:

Root cause confirmation
Code references
Proposed architecture
Fix design
Proof after fix