# 🧾 FTD-010 — UNBLOCK ARCHITECTURE (SCAN vs GATE FIX)

### Ref: FTD-010-EOW-PIPELINE-UNBLOCK (BLACKBOX)

---

# 🎯 OBJECTIVE

Fix critical design flaw:

```text
Gate is blocking signal generation → NO trades possible
```

---

# 🚨 ROOT PROBLEM (CONFIRMED)

Current system:

```text
Global Gate → Scan Controller → Signal Generation (BLOCKED)
```

Result:

```text
No scan → No signal → No trade → Dead system
```

---

# 🧠 TARGET ARCHITECTURE (MANDATORY)

```text
Scan Engine → ALWAYS RUNS
↓
Signal Generated
↓
Gate Check
↓
IF allowed → Execute Trade
ELSE → Log rejection
```

---

# 📁 FILES TO MODIFY (STRICT)

```
/core/profit/scan_controller.py
/core/orchestrator/execution_orchestrator.py
/core/gating/global_gate_controller.py
```

---

# 🔧 FIX 1 — REMOVE SCAN GATING

## 📍 FILE:

```
core/profit/scan_controller.py
```

---

## 🔍 SEARCH:

```python
if not gate_result.allowed:
    return
```

---

## ❌ REMOVE THIS BLOCK COMPLETELY

---

## ✅ REPLACE WITH:

```python
# ALWAYS allow scan to proceed
# Gate will be applied later at execution stage
pass
```

---

## 🧠 INTENT:

```text
Scan MUST run regardless of gate state
```

---

# 🔧 FIX 2 — MOVE GATE TO EXECUTION LEVEL

## 📍 FILE:

```
core/orchestrator/execution_orchestrator.py
```

---

## 🔍 SEARCH:

```python
gate_result = self.global_gate.check(...)
if not gate_result.allowed:
    return
```

---

## ✅ MODIFY TO:

```python
gate_result = self.global_gate.check(...)

if not gate_result.allowed:
    logger.info(f"[EXECUTION-BLOCKED] reason={gate_result.reason}")
    return  # block only execution, NOT scan
```

---

## 🧠 INTENT:

```text
Signal exists → but execution blocked
```

---

# 🔧 FIX 3 — FORCE SIGNAL LOGGING

## 📍 FILE:

```
core/profit/scan_controller.py
```

---

## 🔍 AFTER SIGNAL GENERATED:

ADD:

```python
logger.info(f"[SCAN] Signal generated | symbol={symbol} score={score:.2f}")
```

---

## 🔍 IF SIGNAL REJECTED:

ADD:

```python
logger.info(f"[SCAN] Signal rejected | reason={reason}")
```

---

## 🧠 INTENT:

```text
No more black box — full visibility
```

---

# 🔧 FIX 4 — BOOT GRACE OVERRIDE

## 📍 FILE:

```
core/gating/global_gate_controller.py
```

---

## 🔍 SEARCH:

```python
if not indicators_ready or not data_fresh:
    return GateResult(False, reason)
```

---

## ✅ MODIFY:

```python
if self.system_state == "BOOTING":
    # allow system to warm up
    return GateResult(True, reason="BOOT_GRACE")

if not indicators_ready or not data_fresh:
    return GateResult(False, reason)
```

---

## 🧠 INTENT:

```text
System should NOT block itself during startup
```

---

# 🔧 FIX 5 — ADD SCAN HEARTBEAT

## 📍 FILE:

```
core/profit/scan_controller.py
```

---

## ADD:

```python
logger.debug("[SCAN] Heartbeat — scanning active")
```

---

## 🧠 INTENT:

```text
Confirm scan loop is alive
```

---

# 🧪 VERIFIER SCRIPT (MANDATORY)

## 📍 CREATE FILE:

```
/tests/verify_pipeline_unblock.py
```

---

## CONTENT:

```python
def test_scan_runs_without_gate():
    """
    Ensure scan executes even when gate is blocked
    """
    result = run_engine_simulated(gate_allowed=False)
    assert result.signals_generated > 0

def test_execution_blocked():
    """
    Ensure trades are NOT executed when gate is blocked
    """
    result = run_engine_simulated(gate_allowed=False)
    assert result.trades_executed == 0

def test_signal_logging():
    """
    Ensure signal logs exist
    """
    logs = capture_logs()
    assert "[SCAN] Signal generated" in logs
```

---

# 📦 DEPLOYMENT RULES

Developer MUST:

* Replace full logic blocks (no patching hacks)
* Ensure logs visible at runtime
* Ensure no regression in state engine
* Provide BEFORE vs AFTER logs

---

# 📊 SUCCESS CRITERIA

After fix, logs MUST show:

```text
[SCAN] Signal generated
[EXECUTION-BLOCKED] reason=...
```

Even when:

```text
can_trade=False
```

---

# 🚨 FAILURE CONDITION

If after fix:

```text
No signal logs appear
```

👉 Fix is INVALID

---

# 🧠 FINAL PRINCIPLE

```text
Signal generation = ALWAYS ON
Execution = CONTROLLED
```

---

# 🏁 FINAL LINE

```text
Gate ka kaam hai rokna execution ko

Signal ko janam lene se rokna nahi
```

---
