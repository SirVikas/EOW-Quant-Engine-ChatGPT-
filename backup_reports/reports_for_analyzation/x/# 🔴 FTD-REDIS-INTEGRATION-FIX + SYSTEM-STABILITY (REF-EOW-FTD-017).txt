# 🔴 FTD-REDIS-INTEGRATION-FIX + SYSTEM-STABILITY (REF: EOW-FTD-017)

## 📅 Date

2026-04-16

---

# 🧠 CONTEXT (WHAT IS ALREADY BUILT)

Developer must understand:

System already includes:

* Redis installed as Windows Service (Auto Start + Auto Restart)
* Python-based Redis health monitor (Telegram alerts)
* Task Scheduler automation (pending activation)
* EOW Engine running in PAPER mode
* SQLite Data Lake active
* WebSocket stabilizer present
* Genome engine + self-healing active

BUT:

❌ Redis NOT connected to engine
❌ Metrics showing severe instability
❌ Strategy performance breakdown
❌ Incorrect UI risk signals

---

# 🚨 ALL IDENTIFIED ERRORS (MANDATORY FIX)

## 🔴 CATEGORY A — INFRASTRUCTURE ERRORS

### 1. Redis NOT AVAILABLE (CRITICAL)

* Engine showing:

  ```
  Redis: NOT_AVAILABLE ❌
  ```
* But Redis is actually running

👉 Root Issue:

* Redis connection layer broken

---

### 2. Redis TimeoutError

```
[REDIS-HEALTH] Probe failed: TimeoutError
```

👉 Cause:

* Connection config mismatch / no retry / blocking timeout

---

### 3. In-Memory Fallback Triggered

```
running in-memory only
```

👉 Impact:

* No persistence
* No cross-module sync
* Not production-safe

---

## 🔴 CATEGORY B — ENGINE LOGIC ERRORS

### 4. Genome Timestamp Corruption

```
saved 29605227.0 min ago
```

👉 Cause:

* Time unit mismatch (minutes vs seconds)

---

### 5. Boot Status Mismatch

* Redis running but UI shows NOT_AVAILABLE

👉 Cause:

* Health check not wired to actual Redis connection state

---

## 🔴 CATEGORY C — PERFORMANCE / STRATEGY ERRORS (FROM UI)

### 6. Negative Risk Metrics (CRITICAL)

From screenshot:

* Sortino: -3.533 ❌
* Sharpe: -4.471 ❌
* Calmar: -5.252 ❌

👉 Meaning:

* Strategy losing money consistently
* Risk-adjusted returns negative

---

### 7. Risk of Ruin = 100% ❌

👉 CRITICAL FAILURE:

* System predicts total capital loss

---

### 8. Deployability Index = 33 (NOT READY) ❌

Breakdown:

* Network: 18/30
* Database: 15/30
* RR Edge: 0/40

👉 RR Edge = ZERO → biggest issue

---

### 9. Strategy Rejection / Skip

```
REGIME_UNSTABLE(conf=0.38,ticks=14)
RR achieved: —   Required: —
Gap: —
```

👉 Meaning:

* Strategy cannot meet minimum RR
* Signal quality broken

---

### 10. PnL Degradation

* Capital: 1000 → 924.60
* Net PnL: -75.40
* Profit Factor: 0.36 ❌

👉 System is losing consistently

---

## 🟡 CATEGORY D — SYSTEM QUALITY ISSUES

### 11. WebSocket Initial Disconnect

(Recovered later — still needs stabilization)

---

### 12. Redis Health Detection Not Synced

* Alert system works
* Engine health check fails

👉 Two parallel systems → not unified

---

# 🎯 OBJECTIVE OF THIS FTD

👉 Convert system from:

❌ Semi-functional
❌ Misreporting
❌ Unstable

➡️ TO:

✅ Fully integrated
✅ Correctly reporting
✅ Strategy-safe
✅ Deployable

---

# 🔧 IMPLEMENTATION PLAN (MANDATORY)

---

## 🔥 PHASE 1 — REDIS INTEGRATION FIX (HIGHEST PRIORITY)

### Target File:

* `core/redis_health.py`
* `core/market_data.py`
* `core/bootstrap/api_loader.py`

---

### REQUIRED FIXES:

1. Replace current Redis connection with:

```python
import redis

redis_client = redis.Redis(
    host="localhost",
    port=6379,
    socket_connect_timeout=2,
    socket_timeout=2,
    retry_on_timeout=True
)
```

---

2. Add HARD validation:

```python
def check_redis():
    try:
        redis_client.ping()
        return True
    except Exception:
        return False
```

---

3. REMOVE in-memory fallback trigger unless Redis fails 3 retries

---

4. Sync Boot Status with real Redis state

---

---

## 🔥 PHASE 2 — HEALTH SYSTEM UNIFICATION

👉 Merge:

* Python alert script
* Engine health check

👉 Create:
`core/infra_health_manager.py`

---

### Responsibilities:

* Redis health
* WebSocket health
* API health

---

---

## 🔥 PHASE 3 — GENOME TIME FIX

Replace:

```python
minutes = timestamp
```

With:

```python
minutes = (current_time - saved_time) / 60
```

---

---

## 🔥 PHASE 4 — STRATEGY QUALITY FIX (CRITICAL)

### 1. Enforce MINIMUM RR FILTER

```python
if rr < 1.5:
    reject_signal()
```

---

### 2. Add confidence floor

```python
if confidence < 0.5:
    reject_signal()
```

---

### 3. Disable unstable regimes

```python
if regime == "UNSTABLE":
    skip_trade()
```

---

---

## 🔥 PHASE 5 — RISK ENGINE HARD GUARD

### Add:

```python
if risk_of_ruin > 0.3:
    reduce_position_size()
```

---

### Add kill-switch:

```python
if drawdown > 20%:
    stop_trading()
```

---

---

## 🔥 PHASE 6 — UI METRIC CORRECTION

👉 Fix calculation sources for:

* Sharpe
* Sortino
* Calmar

Ensure:

* Correct rolling window
* No NaN / division error

---

---

## 🔥 PHASE 7 — DEPLOYABILITY INDEX FIX

### Must achieve:

| Metric   | Target |
| -------- | ------ |
| Network  | 25+    |
| Database | 25+    |
| RR Edge  | >20    |

---

---

# 🧪 TESTING REQUIREMENTS (MANDATORY)

Developer must deliver:

### 1. Redis Integration Test

* Redis ON → detected
* Redis OFF → fallback + alert

---

### 2. Strategy Test

* RR > 1.5 enforced
* No unstable trades executed

---

### 3. Risk Test

* Risk of ruin < 50%
* Drawdown control active

---

### 4. UI Validation

* No negative absurd metrics
* Deployability > 60

---

---

# 📂 FILE DELIVERY STRUCTURE

Developer MUST deliver:

```
core/
 ├── redis_health.py (REPLACED)
 ├── infra_health_manager.py (NEW)
 ├── strategy_engine.py (UPDATED)
 ├── risk_engine.py (UPDATED)
 ├── metrics_engine.py (UPDATED)

tests/
 ├── test_redis_integration.py
 ├── test_strategy_quality.py
 ├── test_risk_engine.py
```

---

---

# 📊 EXPECTED FINAL STATE

| Component         | Status      |
| ----------------- | ----------- |
| Redis Integration | ✅           |
| Health System     | ✅ Unified   |
| Strategy Quality  | ✅ Stable    |
| Risk Engine       | ✅ Protected |
| UI Metrics        | ✅ Accurate  |
| Deployability     | ✅ READY     |

---

# 🔐 FINAL RULE

👉 Developer must NOT patch partially
👉 Full module replacement required

---

# 💥 CORE PRINCIPLE

“System must NEVER trade unless it is statistically safe, infra-stable, and fully observable.”

---
