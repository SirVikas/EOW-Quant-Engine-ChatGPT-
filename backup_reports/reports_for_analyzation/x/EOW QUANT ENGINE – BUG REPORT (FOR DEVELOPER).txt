**EOW QUANT ENGINE – BUG REPORT (FOR DEVELOPER)**
**(PRODUCTION CRITICAL OBSERVATIONS FROM LOG ANALYSIS)**

---

### 🔴 1. REDIS STATUS INCONSISTENCY (CRITICAL)

**Issue:**
System checks show:

```
Redis : connected
```

But later:

```
Boot Status → Redis: NOT_AVAILABLE ❌
```

Then again:

```
[MDP] Redis connected — pub/sub active.
```

**Problem:**

* Multiple Redis states → inconsistent health reporting
* Indicates multiple connection layers OR race condition

**Fix Required:**

* Centralize Redis connection (single source of truth)
* Remove duplicate health checks
* Sync:

  * system_check
  * bootstrap
  * runtime health

---

### 🔴 2. WEBSOCKET STATUS FALSE NEGATIVE

**Issue:**

```
Boot Status → WebSocket: DISCONNECTED ❌
```

But actual logs:

```
[MDP] WebSocket connected.
[WS-STAB] Stream healthy
```

**Problem:**

* UI/Boot status is WRONG
* Monitoring unreliable

**Fix Required:**

* Bind WebSocket status to actual connection flag
* Avoid static initialization state

---

### 🔴 3. DNA RESTORE TIME ANOMALY

**Issue:**

```
DNA restored from disk (saved 29605935.9 min ago)
```

**Problem:**

* ~56 YEARS → impossible
* Timestamp corruption / wrong unit

**Fix Required:**

* Validate timestamp format
* Use:

```python
datetime.now() - saved_time
```

* Ensure milliseconds vs minutes mismatch not happening

---

### 🔴 4. ZERO / INVALID MARKET DATA (ADX = 0)

**Issue:**
Repeated:

```
ADX=0.0 → WEAK
```

**Problem:**

* Indicator receiving incomplete data
* Strategy making decisions on invalid signals

**Fix Required:**

* Add validation:

```python
if adx <= 0:
    reject_signal()
```

* Ensure minimum candle buffer BEFORE indicator calc

---

### 🔴 5. EXCESSIVE REGIME RE-CALCULATION (PERFORMANCE BUG)

**Issue:**
Same symbol processed 20+ times in milliseconds

**Problem:**

* CPU wastage
* Over-triggering strategies

**Fix Required:**

* Add debounce / throttling:

```python
if last_updated < 500ms:
    skip
```

---

### 🔴 6. HIGH FEE RATIO NOT BLOCKING TRADE (LOGIC BUG)

**Issue:**

```
HIGH_FEE_RATIO (fees = 72.7% of profit)
```

But still:

```
Signal LONG generated
```

**Problem:**

* Loss-making trades allowed

**Fix Required:**

* Hard block:

```python
if fee_ratio > threshold:
    block_trade = True
```

---

### 🔴 7. DATA INSUFFICIENCY ERROR (DATA_001)

**Issue:**

```
Missing or insufficient candle data | sym=LINKUSDT
```

**Problem:**

* System still running strategy engine

**Fix Required:**

* Add:

```python
if candles < required:
    disable_symbol()
```

---

### 🔴 8. UNKNOWN REGIME LOOP (STRAT_001)

**Issue:**

```
Regime classified as UNKNOWN
Repeated fallback with penalty
```

**Problem:**

* Infinite fallback loop
* Strategy instability

**Fix Required:**

* Add max retry limit
* Default safe regime:

```python
if unknown_count > 3:
    regime = NO_TRADE
```

---

### 🔴 9. ADX CLAMPING LOGIC ISSUE

**Issue:**

```
ADX=100 → clamped to 60
```

**Problem:**

* Artificial distortion of market strength

**Fix Required:**

* Instead of clamp:

  * Normalize or scale properly
  * OR validate data source

---

### 🔴 10. PROFIT FACTOR CRITICAL (PF < 1)

**Issue:**

```
PF = 0.363 after 38 trades
```

**Problem:**

* Strategy is losing money

**Fix Required:**

* Auto-disable strategy:

```python
if PF < 1:
    reduce_trade_frequency()
    OR stop strategy
```

---

### 🔴 11. SYMBOL QUALITY ISSUE (MICROCAP / BAD SYMBOL)

**Issue:**

```
NEIROUSDT → extremely low price, huge quantity
```

**Problem:**

* High slippage
* Unrealistic execution

**Fix Required:**

* Add symbol filter:

```python
if price < threshold:
    skip_symbol
```

---

### 🔴 12. BOOT STATUS VS ACTUAL STATE MISMATCH

**Issue:**
Boot shows:

```
Deployability: NOT_READY (43/100)
```

But system running fully

**Problem:**

* Misleading health reporting

**Fix Required:**

* Align scoring logic with runtime state

---

### 🟡 13. LOG FLOODING (DEBUG OVERLOAD)

**Issue:**
Hundreds of repetitive logs

**Problem:**

* Hard to debug real issues
* Performance overhead

**Fix Required:**

* Log sampling / rate limit

---

### 🟡 14. NO TRADE CONTROL ON LOW VOLATILITY

**Issue:**

```
LOW_VOL_TRAP → tradeable=False
```

But still evaluations happening

**Fix Required:**

* Strict enforcement:

```python
if not tradeable:
    skip_signal_generation
```

---

## ✅ FINAL SUMMARY

**Critical Risk Areas:**

* Redis / WebSocket state mismatch
* Wrong market data (ADX=0)
* Loss-making strategy still active
* Fee logic ignored
* Infinite regime loops

---

## 🚨 PRIORITY

**HIGH PRIORITY (Immediate Fix):**

1. Redis inconsistency
2. Fee logic bug
3. Invalid data handling (ADX=0)
4. Unknown regime loop

**MEDIUM:**
5. Performance (recalculation)
6. Logging
7. Symbol filtering

---

**This system is RUNNING — but NOT RELIABLE yet.**
Production deployment in current state = HIGH RISK.
