# 🧾 qFTD-011 — SIGNAL GENERATION FAILURE (FORENSIC LEVEL)

### Ref: qFTD-011-EOW-ALPHA-ENGINE-BLOCK

---

# 🎯 OBJECTIVE

Resolve:

```text
System scanning correctly but producing ZERO signals
```

---

# 🚨 SECTION 1 — OBSERVED REALITY (CONFIRMED)

From runtime:

```text
[SCAN-CTRL] scanning active (can_trade=True)
[DATA-HEALTH] OK
[INDICATOR-VALIDATOR] OK
[BOOT-DEPLOY] READY
```

BUT:

```text
NO:
- [SCAN] Signal generated
- EV logs
- Score logs
- Trade attempts
```

---

## ❗ CRITICAL CONCLUSION

```text
Signal generation layer is NOT producing output
```

---

# 🧠 SECTION 2 — TARGET LAYER

Primary suspect:

```text
core/strategies/alpha_engine.py
```

Secondary:

```text
scan_controller → strategy invocation
```

---

# 🔍 SECTION 3 — DEVELOPER MUST ANSWER (MANDATORY)

---

## Q1 — STRATEGY INVOCATION (MOST CRITICAL)

Provide exact flow:

```text
scan_controller →
    ??? →
        alpha_engine.generate_signals()
```

👉 Answer:

```text
Is alpha_engine being called on EVERY tick?
YES / NO
```

---

## Q2 — SIGNAL COUNT (REAL OUTPUT)

Add temporary debug and report:

```python
signals = alpha_engine.generate_signals(...)
logger.info(f"[DEBUG] signals_found={len(signals)}")
```

👉 Provide:

```text
Actual values for 10 consecutive ticks
```

---

## Q3 — STRATEGY CONDITIONS

For each strategy:

* TrendFollowing
* MeanReversion
* VolatilityExpansion

👉 Provide:

```text
Exact entry conditions (ALL filters)
```

Example:

```text
RSI < ?
ADX > ?
Volume condition?
RR requirement?
```

---

## Q4 — CONDITION FAILURE REASONS

For at least 1 symbol (BTCUSDT):

Provide:

```text
Why signal NOT generated?
```

Format:

```text
RSI condition failed (value=52, required<30)
ADX condition failed (value=18, required>20)
RR failed (1.2 < 2.0)
```

---

## Q5 — INDICATOR VALUES SNAPSHOT

Provide real-time values:

```text
Symbol: BTCUSDT

RSI = ?
ADX = ?
ATR = ?
Volume score = ?
```

---

## Q6 — RR ENGINE BLOCK

Confirm:

```text
Does RR engine reject signals BEFORE they are logged?
YES / NO
```

---

## Q7 — SCORING LAYER

Confirm:

```text
Is trade_scorer executed if signal count = 0?
```

---

## Q8 — FILTER CASCADE

Provide pipeline:

```text
Signal →
    Indicator filter →
    RR filter →
    Score filter →
    EV filter →
    Execution
```

👉 Answer:

```text
At which stage signals die?
```

---

## Q9 — LOGGING COVERAGE

Confirm:

```text
Are rejected signals logged?
YES / NO
```

If NO → why?

---

## Q10 — EDGE THRESHOLDS

Provide current thresholds:

```text
min_score =
min_rr =
min_ev =
```

---

# ⚠️ SECTION 4 — HYPOTHESIS TO VERIFY

We suspect:

```text
Conditions too strict → zero signals
```

OR

```text
Strategy not invoked properly
```

---

# 🔧 SECTION 5 — REQUIRED DEBUG ACTIONS

Developer MUST:

---

## ✅ ACTION 1 — FORCE LOG

Add:

```python
logger.info("[DEBUG] Entering alpha_engine")
```

---

## ✅ ACTION 2 — LOG ALL CONDITIONS

```python
logger.info(f"RSI={rsi} ADX={adx} ATR={atr}")
```

---

## ✅ ACTION 3 — LOG REJECTION

```python
logger.info(f"[REJECT] reason={reason}")
```

---

## ✅ ACTION 4 — TEMP RELAXATION TEST

Set:

```text
min_score → 0.1
min_rr → 1.0
```

👉 Then report:

```text
Do signals appear?
YES / NO
```

---

# 📦 REQUIRED aFTD OUTPUT

Developer MUST provide:

1. Signal count logs
2. Strategy condition breakdown
3. Indicator values
4. Rejection reasons
5. Pipeline stage of failure
6. Relaxation test result

---

# 🧠 FINAL PRINCIPLE

```text
If system sees market but finds ZERO opportunity,
problem is NOT market — problem is strategy design
```

---

# 🏁 FINAL LINE

```text
Signal generation = heart of trading system

If heart not beating → system cannot trade
```

---
